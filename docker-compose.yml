services:
  redis:
    image: redis:7-alpine
    command: redis-server --appendonly yes --requirepass ${REDIS_PASSWORD}
    ports:
      - "${VUECUTTER_REDIS_PORT:-6379}:6379"
    volumes:
      - redis-data:/data

  backend:
    build:
      context: .
      dockerfile: docker/backend.Dockerfile
    privileged: true
    depends_on:
      - redis
    environment:
      IN_DOCKER: "true"
      PLEXDATA_CONFIG: /app/config.toml
      PLEX_SERVERS_CONFIG: /app/servers.json
      REDIS_HOST: redis
      REDIS_PORT: "6379"
      REDIS_PASSWORD: ${REDIS_PASSWORD}
      VUECUTTER_REDIS_PASSWORD: ${REDIS_PASSWORD}
      VUECUTTER_MEDIA_KEEP_SHARE: ${VUECUTTER_MEDIA_KEEP_SHARE:-true}
    ports:
      - "${VUECUTTER_BACKEND_PORT:-5200}:5200"
    volumes:
      - ./VueCutter:/app
      - ./docker:/app/docker
      - ${HOST_CONFIG_PATH:-./VueCutter/config.toml}:/app/config.toml:ro
      - ${HOST_SERVERS_PATH:-./VueCutter/servers.json}:/app/servers.json:ro
      - /mnt/media:/mnt/media
      - type: bind
        source: /etc/smbcredentials
        target: /etc/smbcredentials
        read_only: true
        bind:
          create_host_path: false

  worker:
    build:
      context: .
      dockerfile: docker/backend.Dockerfile
    privileged: true
    depends_on:
      - redis
    command: ["/app/docker/worker-entrypoint.sh"]
    environment:
      IN_DOCKER: "true"
      PLEXDATA_CONFIG: /app/config.toml
      PLEX_SERVERS_CONFIG: /app/servers.json
      REDIS_HOST: redis
      REDIS_PORT: "6379"
      REDIS_PASSWORD: ${REDIS_PASSWORD}
      VUECUTTER_REDIS_PASSWORD: ${REDIS_PASSWORD}
      VUECUTTER_MEDIA_KEEP_SHARE: ${VUECUTTER_MEDIA_KEEP_SHARE:-true}
    volumes:
      - ./VueCutter:/app
      - ./docker:/app/docker
      - ${HOST_CONFIG_PATH:-./VueCutter/config.toml}:/app/config.toml:ro
      - ${HOST_SERVERS_PATH:-./VueCutter/servers.json}:/app/servers.json:ro
      - /mnt/media:/mnt/media
      - type: bind
        source: /etc/smbcredentials
        target: /etc/smbcredentials
        read_only: true
        bind:
          create_host_path: false

  frontend:
    build:
      context: .
      dockerfile: docker/frontend.Dockerfile
    depends_on:
      - backend
    environment:
      # API_PROXY_BASE: http://backend:5200
      API_PROXY_BASE: ${API_PROXY_BASE:-http://backend:5200}
      NUXT_PUBLIC_API_BASE: ""
    ports:
      - "${VUECUTTER_FRONTEND_PORT:-8200}:3000"
    volumes:
      - ./frontend:/app
      - frontend-node-modules:/app/node_modules
      - frontend-nuxt:/app/.nuxt

volumes:
  redis-data:
  frontend-node-modules:
  frontend-nuxt:
